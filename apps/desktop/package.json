{
  "name": "@project/desktop",
  "private": true,
  "version": "0.1.0",
  "author": "moonrock-software",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./out/main/main.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "format": "biome format --write src",
    "typecheck": "tsc --noEmit",
    "test": "playwright test --project unit --project browser",
    "test:e2e": "electron-vite build && xvfb-run -a playwright test --project e2e",
    "test:installed": "SYSTEM_AGENT_EXECUTABLE=/usr/bin/system-agent xvfb-run -a playwright test --project installed",
    "clean": "rm -rf out dist .turbo test-results node_modules/.vite",
    "smoke": "npm run typecheck && npm run lint && npm run test && npm run test:e2e && npm audit",
    "package": "rm -rf dist && electron-vite build && electron-builder --config electron-builder.config.cjs --linux",
    "verify:package": "node ../../scripts/verify-package.mjs"
  },
  "dependencies": {
    "@electron-toolkit/utils": "^4.0.0",
    "@project/hooks": "*",
    "@project/types": "*",
    "@project/ui": "*",
    "@tailwindcss/vite": "^4.3.2",
    "lucide-react": "^1.24.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@playwright/test": "^1.61.1",
    "@types/node": "24.13.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "electron": "^43.1.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "5.0.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  },
  "turborepo": {
    "tags": [
      "app"
    ]
  },
  "description": "Desktop system manager for Arch Linux",
  "homepage": "https://moonrock.software"
}
